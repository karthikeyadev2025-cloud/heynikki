{
  "name": "heynikki-app",
  "version": "1.0.0",
  "private": true,
  "description": "Hey Nikki — the business owner's phone app. Wraps the live dashboard and runs the always-on “Hey Nikki” listener.",
  "scripts": {
    "sync": "cap sync android",
    "android": "cap open android",
    "apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2"
  }
}
