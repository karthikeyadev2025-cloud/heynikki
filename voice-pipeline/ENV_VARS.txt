# ═══════════════════════════════════════════════════
# HEY NIKKI VOICE PIPELINE — Environment Variables
#
# Replaces the old RAILWAY_ENV_VARS.txt. Railway was dropped in the v4.0
# architecture (everything backend now runs on the Mumbai EC2 box), and
# LiveKit was removed from the pipeline entirely in July — both files
# were still telling you to configure services that are no longer used.
#
# Set these in the EC2 host's .env, NOT in git. Real values live in your
# password manager.
# ═══════════════════════════════════════════════════

# ── Database ──────────────────────────────────────
SUPABASE_URL=https://<your-project>.supabase.co
SUPABASE_SERVICE_KEY=<service_role_key_from_supabase_settings>

# ── AI vendors ────────────────────────────────────
# Required. /api/public/voice-turn returns 503 without SARVAM_API_KEY,
# which means the landing-page call console goes silent.
SARVAM_API_KEY=<your_sarvam_key>
GEMINI_API_KEY=<your_gemini_key>

# ── Internal service auth ─────────────────────────
# Shared between voice-pipeline and api-server. Must match on both.
INTERNAL_SECRET=<generate_a_long_random_string>

# Where the API server is reachable from the pipeline.
API_SERVER_URL=http://127.0.0.1:4000

# ── FreeSWITCH ────────────────────────────────────
# No default any more: the code used to fall back to FreeSWITCH's stock
# "ClueCon" password, so a missing value silently produced a working ESL
# connection secured by a password everyone knows. It now fails closed,
# so click-to-call and the Super Admin FreeSWITCH panel need this set.
FREESWITCH_HOST=127.0.0.1
FREESWITCH_ESL_PORT=8021
FREESWITCH_ESL_PASSWORD=<set_this_and_match_event_socket.conf.xml>

# ── SIP trunks ────────────────────────────────────
# From your Jio enterprise onboarding pack. If Jio uses IP-based auth
# rather than registration, username/password are unused — see
# infra/freeswitch/conf/sip_profiles/heynikki_trunks.xml.
JIO_SIP_HOST=
JIO_SIP_USERNAME=
JIO_SIP_PASSWORD=

VI_SIP_HOST=
VI_SIP_USERNAME=
VI_SIP_PASSWORD=

# ── Recording storage (Cloudflare R2) ─────────────
CF_ACCOUNT_ID=
R2_ACCESS_KEY=
R2_SECRET_KEY=
R2_BUCKET=heynikki-recordings

# ── Telephony fallback (optional) ─────────────────
# Only needed if the Super Admin telephony toggle is set to Exotel.
EXOTEL_SID=
EXOTEL_API_KEY=
EXOTEL_API_TOKEN=
EXOTEL_WEBHOOK_TOKEN=

# ── Email / site ──────────────────────────────────
FROM_EMAIL=hello@heynikki.in
SITE_URL=https://heynikki.in
