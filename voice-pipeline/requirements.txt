fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
python-multipart==0.0.12
python-dotenv==1.0.1
cryptography>=42.0.0
sentry-sdk[fastapi]>=2.0.0
# Cloudflare R2 recording upload (v4.0)
boto3>=1.35.0
botocore>=1.35.0
# livekit / livekit-agents / livekit-plugins-silero removed 2026-07-25:
# dead code, never called at runtime, and the source of fresh-install
# failures (documented version-pin conflict). The pipeline uses a custom
# RMS VAD and raw WebSocket audio — no LiveKit runtime dependency.

# Test-only. Kept in the image so the call-flow suite can run before a
# deploy without a network install — every bug so far was found by a human
# placing a phone call, which is the thing these tests replace.
pytest==8.3.4
