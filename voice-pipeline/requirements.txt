fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
# BUGFIX (found live, 2026-07-02): livekit==0.17.5 conflicts with
# livekit-agents==0.8.0, which requires livekit~=0.12.0.dev1. This never
# surfaced before because Sydney's venv was built incrementally over many
# ad-hoc sessions rather than from a clean `pip install -r requirements.txt`
# — a fresh install (e.g. for the India region migration) failed outright
# with ResolutionImpossible. Verified this exact pin resolves cleanly.
livekit~=0.12.0.dev1
livekit-agents==0.8.0
livekit-plugins-silero==0.6.0
python-multipart==0.0.12
python-dotenv==1.0.1
cryptography>=42.0.0
sentry-sdk[fastapi]>=2.0.0
